# dotfiles-manager

A rust program to manage dotfiles with minimal system dependencies.

## TODO

- parse arguments

- build for linux x86_64/darwin x86_64

- commands list:
  - sync (pull changes and attempt to apply)
  - check (checks for updates)
  - stow (reimplement GNU stow)
  - install (downloads and installs)
  - uninstall (remove config)
